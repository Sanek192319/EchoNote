<Window x:Class="EchoNote.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EchoNote"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="TitleBox"
         Grid.Row="0"
         Margin="0,0,0,10"
         Height="28"
         Text=""
         ToolTip="Введите название записи"/>

        <StackPanel Grid.Row="1"
            Orientation="Horizontal"
            Margin="0,0,0,10">
            <Button Content="Start Recording"
            Width="140"
            Margin="0,0,10,0"
            Click="StartRecording_Click"/>
            <Button Content="Stop &amp; Save"
            Width="120"
            Click="StopRecording_Click"/>
        </StackPanel>

        <ListBox x:Name="TranscriptsList"
         Grid.Row="2"/>

    </Grid>
</Window>
